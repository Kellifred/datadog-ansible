pipeline {
    agent any
    
    stages {
        stage('Clone repository') {
            steps {
                git 'https://github.com/Kellifred/datadog-ansible.git'
            }
        }
    }
}
